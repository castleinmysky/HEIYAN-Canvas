# 更新记录

## 画布 1.2.1 / 开源连接器 1.6.1 · 2026-09-10

- 重新发布与当前开源源码一致的 Windows 免安装包和源码包，附 SHA-256 校验文件。
- 连接器、启动页、更新清单和下载清单使用一致的版本标识；构建时拒绝版本不一致。
- 同步连接器的状态增量查询与已领取操作回执恢复接口，兼容旧版画布；不重放修改或生成。
- 更新包增加开源独立版标识，避免被主平台专用连接器覆盖；不导入主平台账号或任务授权逻辑。
- 包含此前的画布性能及 Agent API 连接修复。[更新与下载说明](docs/releases-1.2.1.md)。

## Agent API 连接补丁 · 2026-09-10

- 连接检测显示当前项目与等待时间：基础检测每项最多 45 秒，可选检测每项最多 20 秒，总计最多 2 分钟。
- 可选检测超时或返回 HTTP 524 时保留已通过的基础连接，明确显示本地参数校验、保守计数或项目摘要等替代方式。
- 取消检测立即结束等待，忽略迟到的响应，不自动重试或切换模型；未通过的可选能力不会被启用。
- 流式回复收到协议结束事件即可完成，不再等待中转站关闭连接；真实流式工具调用与结果回传通过后才启用流式模式。
- 保留自部署接口、现有画布数据和连接器；新增超时、取消、流式结束及检测进度回归测试。

## 画布性能补丁 · 2026-09-10

- 拖动与缩放复用节点元数据，减少重复计算；结束操作时立即保存最终视口。
- 节点回到可见区域时直接复用图片预览，低缩放不因缺少预览而提前挂载原图。
- 同一素材的预览地址共用缓存，保留不同来源和鉴权参数的隔离。
- 仅更新公共渲染逻辑，不修改模型配置、任务数据或连接器。

## 画布 1.2 · 2026-09-10

- 会话气泡、紧凑排版、发送图片缩略图与图片/视频/音频结果浏览。
- 顶部集中会话、记忆、进度、设置入口，预留 Skill 位置；尚未实现 Skill 加载与执行。
- 审批与模型选择集中到发送区；支持按画布保存审批模式，完全访问权限需用户主动开启。
- 项目记忆改为阅读优先、按项编辑与展开；保留未保存的草稿。
- 进度只随真实状态变化更新；续聊减少重复历史，连入节点的 `@` 标记传递给 Agent。
- 修复多图手牌拖拽、连线层级及 `gpt-image-2.5` 比例识别，移除规格菜单中的未开放说明。
- 保留自部署本地存储及开源连接器下载入口，不引入主平台账号与任务接口。[完整说明](docs/releases-1.2.md)。

## 画布 1.1 / 连接器 1.6.0 · 2026-09-08

- 同步最新 Agent 会话界面、模型选择、图片输入、项目记忆、上下文管理、历史检索、修改预览、空间排版和生成结果检查。
- 自部署项目记录按画布保存在当前浏览器，保持免账号；修复会话 API 流式请求被本地存储层截获的问题。
- 升级免安装与源码连接器，生成始终单独确认；已领取的操作不会因重连而自动重放。
- 保留主画布原有生成和节点交互；中英文说明同步更新。[完整更新说明](docs/releases-1.1.md)。

## 连接器 1.3.1 · 2026-09-08

- Agent 读取实时选区，并区分会话引用；支持指定输入端口、断开和批量替换引用、移动、选择、复制及删除节点。
- Agent 可读取可用生成模型与参数范围，设置标准生成节点的模型、比例、分辨率、数量与视频时长。
- 修复生成历史图片预览的居中布局，避免原始图片尺寸撑开预览区域。
- 1.3.0 启动器重启时可获取增量更新；分组、遮罩和专用 3D 操作尚未全面接入 Agent。

## 连接器 1.3.0 · 2026-09-08

- Agent 会话可读取当前 Codex 账号实际可用的模型和思考程度，不使用写死的模型列表。
- 支持把 PNG、JPEG 或 WebP 图片直接粘贴到会话，由 Agent 结合图片与文字需求分析；画布原始素材不会自动发送。
- 修复 Agent 创建连线时使用无效端口的问题，并向 Agent 补充节点输入端口、输出类型和连线端口元数据。
- Windows 连接器加入带校验的增量更新与离线回退；升级到 1.3.0 完整包一次后，普通连接器更新不再需要反复下载完整运行时。

## 连接器 1.2.0 · 2026-09-08

- 修复 Agent 配对链接回到画布首页的问题，`?view=agent` 会直接进入创作画布。
- 新增“请求批准 / 帮我批准”分级审批；自动模式仅执行创建和连线等可逆操作。
- Agent 可在画布中集中请求最多 4 个已存在的图片、视频、音频、3D 或 ComfyUI 生成节点。
- 真实生成不受自动批准影响，永远显示资源消耗提醒并等待用户明确确认；采用单次领取和结果回传防止重复提交。
- Windows 连接器升级为协议 2 / 版本 1.2.0，更新免安装包、源码包与离线验收流程。

## 1.0.0 · 2026-09-07

- 独立画布：节点编辑、混合引用、成组、批量连线、多图手牌、按画布生成历史。
- 图片、视频、音频、3D 的现有模型适配与用户自有 API 高级连接。
- 个人 Codex 连接、会话、节点操作确认，以及 Windows 免安装连接器。
- ComfyUI 本机连接、现有模型和工作流适配、可选受保护的临时跨网络通道。
- 独立 Node.js 启动入口、配置示例、部署说明、纯 Markdown 功能展示和回归测试。

不包含作者账号、私人任务或模型权重。真实生成仍需用户准备服务与权限；账号云同步、手机远程 Codex 和自动完成整部短片不在本版范围内。
