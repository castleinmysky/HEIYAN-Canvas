# 参与开发

先安装 Node.js >=22.13，运行 `npm ci`，再用 `npm run dev` 启动画布。

一项修改尽量对应一个问题。模型适配放在已有接口内，交互修改同时检查桌面和窄屏，不要用新账号系统替换浏览器本地数据边界。

## 提交前

```bash
npm run check
npm test
npm run build
npm run check:release
npm audit
```

测试使用模拟接口或隔离目录，不自动请求付费服务。涉及真实生成的验收应由使用者明确确认。提交时说明改了什么、测试结果及未覆盖的环境。

不要提交密钥、配对码、Codex 登录态、个人任务、真实日志、运行时缓存和模型权重。配置示例只用占位值；截图先核对私人信息。

## 代码与目录

`src/` 是界面与浏览器状态，`shared/` 是模型和连接协议，`server/` 是独立服务及 Codex 连接器，`local-bridge/` 是 ComfyUI 接入。发布脚本在 `scripts/`；文档配图统一放 `docs/media/`。

README 使用纯 Markdown，功能图集中在多列预览表中。不要重新引入 HTML 布局标签、开发记录或作者机器路径。

修改 README 或快速开始时，请同步中英文版本；保留顶部语言切换，并共用相同的截图和 GIF，不重复复制媒体文件。

提交代码时遵循仓库的 Apache-2.0 许可，并保留第三方声明。不要将不具备分发权限的媒体或依赖加入仓库。
