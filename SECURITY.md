# 安全与隐私

默认只监听本机。对外部署应使用 HTTPS、独立部署密码和防火墙；它不是多租户账号服务，也不适合直接作为匿名公共 API 代理。

## 数据边界

- 画布和模型设置主要保存在当前浏览器。浏览器本地存储不是密钥保险库，避免在共享设备保存长期凭据。
- 云 API 请求会经所连接的供应商、配置的中转站，以及必要的自托管转发服务处理。部署者可能接触传输中的内容，只使用可信部署。
- Codex 登录由 Codex 负责；连接器仅配对当前来源和画布，不分发作者账号。配对码和本机状态需要保密。
- ComfyUI 跨网络模式使用独立访问码。临时通道会经过 Cloudflare，提示词、输入素材和结果也会经过它。
- 不确定的生成提交不自动重试，避免重复扣费。浏览器刷新不代表上游任务被取消。

## 报告问题

不要在 Issue、PR 或日志中公开密钥、登录文件、可用配对码或私人作品。先撤销泄露的凭据，再提供脱敏的复现步骤。

如仓库已启用 GitHub 私密漏洞报告，使用 Security → Report a vulnerability；否则先用不包含利用细节或秘密信息的 Issue 联系维护者，取得私下渠道后再发送完整报告。

自动化发布检查不是完整安全审计。部署者仍需管理 HTTPS、操作系统权限、依赖更新、访问范围和备份。
