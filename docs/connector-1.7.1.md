# HEIYAN 开源连接器 1.7.1

- 保留 1.7.0 的问题卡、能力发现、受约束能力调用和 ComfyUI/H3 协作能力。
- 取消节点读取、批量编辑、生成提交和结果回读的任意数量上限。
- 保留生成队列、并发、额度、审批与稳定性保护；“不限数量”不等于无限并发。
- Windows 包名包含版本号，压缩包根目录包含 `VERSION.txt`。
- 更新清单继续锁定 `heiyan-standalone`，只接受与当前发行渠道匹配的连接器更新。

本次更新覆盖 Agent、连接器与画布视觉能力；运行数据和账号凭据不进入发行包。
